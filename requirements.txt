# Core dependencies
pymupdf>=1.24.0      # PDF processing (import as fitz)
fastapi>=0.109.0     # Web framework
uvicorn>=0.27.0      # ASGI server
python-multipart>=0.0.6  # File upload handling
pydantic>=2.0.0      # Structured output models

# OpenAI SDK (used for DeepSeek API - OpenAI-compatible)
openai>=1.0.0        # For DeepSeek API integration

# MCP (Model Context Protocol) for custom tools
mcp>=1.0.0

# OpenParse for parsing context files (PDF, PPTX, DOCX, images)
# Open-source, no API key required
openparse>=0.1.0

# HTTP client for API key validation
httpx>=0.27.0

# Environment variable management (.env file support)
python-dotenv>=1.0.0

# =============================================================================
# Optional: GLM-ASR Speech Recognition (语音转文字)
# https://github.com/zai-org/GLM-ASR
# =============================================================================
# Note: GLM-ASR requires transformers 5.0.0 from source:
#   pip install git+https://github.com/huggingface/transformers
# Also requires:
#   torch (with CUDA for GPU acceleration)
#   ffmpeg (system package: apt install ffmpeg / brew install ffmpeg)
# 
# Uncomment below to install torch (choose appropriate version for your CUDA):
# torch>=2.0.0
